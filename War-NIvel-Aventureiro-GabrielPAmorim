#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

// Definição da estrutura Territorio
typedef struct {
    char nome[30];
    char cor[10]; // Cor (dono) do exército: ex: "Azul", "Vermelho"
    int tropas;
} Territorio;

// Protótipos das funções
Territorio* alocarMapa(int *numTerritorios);
void cadastrarTerritorios(Territorio* mapa, int numTerritorios);
void exibirTerritorios(const Territorio* mapa, int numTerritorios);
void atacar(Territorio* atacante, Territorio* defensor);
void liberarMemoria(Territorio* mapa);
int rolarDado();
int buscarTerritorio(const Territorio* mapa, int numTerritorios, const char* nome);

// --- Função Principal ---
int main() {
    // Inicializa o gerador de números aleatórios para a simulação de dados
    srand(time(NULL));

    Territorio* mapa = NULL;
    int numTerritorios = 0;
    int escolhaAtacante, escolhaDefensor;
    char nomeAtacante[30], nomeDefensor[30];

    printf("🗺️ --- Simulador de WAR Estruturado (Ataque) ---\n");

    // 1. Alocação Dinâmica de Territórios
    mapa = alocarMapa(&numTerritorios);
    if (mapa == NULL) {
        return 1; // Encerra se a alocação falhar
    }

    // 2. Cadastro dos Territórios
    cadastrarTerritorios(mapa, numTerritorios);

    // Loop de simulação de ataques
    char continuar = 's';
    while (continuar == 's' || continuar == 'S') {
        printf("\n--- Estado Atual do Mapa ---\n");
        exibirTerritorios(mapa, numTerritorios);

        // 3. Seleção e Validação do Ataque
        printf("\n🔥 Preparando para o Ataque...\n");

        // Seleção do Território Atacante
        do {
            printf("Digite o NOME do território ATACANTE: ");
            if (scanf("%29s", nomeAtacante) != 1) continue; // Garante leitura
            escolhaAtacante = buscarTerritorio(mapa, numTerritorios, nomeAtacante);

            if (escolhaAtacante == -1) {
                printf("❌ Território atacante não encontrado. Tente novamente.\n");
            } else if (mapa[escolhaAtacante].tropas < 2) {
                printf("❌ O atacante precisa de no mínimo 2 tropas para atacar. Tropas atuais: %d. Tente outro.\n", mapa[escolhaAtacante].tropas);
                escolhaAtacante = -1; // Força nova tentativa
            }
        } while (escolhaAtacante == -1);

        // Seleção do Território Defensor
        do {
            printf("Digite o NOME do território DEFENSOR: ");
            if (scanf("%29s", nomeDefensor) != 1) continue; // Garante leitura
            escolhaDefensor = buscarTerritorio(mapa, numTerritorios, nomeDefensor);

            if (escolhaDefensor == -1) {
                printf("❌ Território defensor não encontrado. Tente novamente.\n");
            } else if (escolhaDefensor == escolhaAtacante) {
                printf("❌ O território defensor não pode ser o mesmo que o atacante. Tente outro.\n");
                escolhaDefensor = -1;
            } else if (strcmp(mapa[escolhaAtacante].cor, mapa[escolhaDefensor].cor) == 0) {
                // Requisito: Validar para que o jogador não ataque um território da própria cor.
                printf("❌ Você não pode atacar um território da sua própria cor (%s). Tente outro.\n", mapa[escolhaAtacante].cor);
                escolhaDefensor = -1; // Força nova tentativa
            }
        } while (escolhaDefensor == -1);

        // 4. Simulação do Ataque
        printf("\n⚔️ SIMULANDO: %s (%s) ataca %s (%s)!\n", 
               mapa[escolhaAtacante].nome, mapa[escolhaAtacante].cor, 
               mapa[escolhaDefensor].nome, mapa[escolhaDefensor].cor);
        
        // Chamada da função de ataque, usando ponteiros para os structs
        atacar(&mapa[escolhaAtacante], &mapa[escolhaDefensor]);

        // 5. Exibição Pós-ataque
        printf("\n--- Resultado do Ataque ---\n");
        printf("Atacante: %s | Cor: %s | Tropas: %d\n", 
               mapa[escolhaAtacante].nome, mapa[escolhaAtacante].cor, mapa[escolhaAtacante].tropas);
        printf("Defensor: %s | Cor: %s | Tropas: %d\n", 
               mapa[escolhaDefensor].nome, mapa[escolhaDefensor].cor, mapa[escolhaDefensor].tropas);
        printf("---------------------------\n");


        printf("\nDeseja realizar outro ataque? (s/n): ");
        scanf(" %c", &continuar);
        // Limpa o buffer de entrada para evitar problemas no próximo loop
        while(getchar() != '\n');
    }

    // 6. Liberação de Memória
    liberarMemoria(mapa);
    printf("\n✅ Memória liberada com sucesso. Fim do programa.\n");
    
    return 0;
}

// --- Funções de Implementação ---

/**
 * @brief Aloca memória dinamicamente para o vetor de Territorios.
 * @param numTerritorios Ponteiro para armazenar o número total de territórios.
 * @return Ponteiro para o primeiro elemento do vetor alocado, ou NULL em caso de falha.
 */
Territorio* alocarMapa(int *numTerritorios) {
    printf("Informe o número total de territórios para o mapa: ");
    if (scanf("%d", numTerritorios) != 1 || *numTerritorios <= 0) {
